Глава 1. Значения, типы и операторы.

Число в JS содержит 64 бита.
NaN - результат операции, которая не дает осмысленного результата.
Строки хранятся в формате Unicode.
При || возвращается первое значение, которое равно true.
При && - которое равно false.